# Core dependencies
PySide6>=6.6.0
Pillow>=10.0.0

# Test dependencies
pytest>=8.0.0
pytest-qt>=4.4.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0

# Build dependencies (optioneel voor het bouwen van de installer DMG)
dmgbuild>=1.6.0
pyinstaller>=6.0.0